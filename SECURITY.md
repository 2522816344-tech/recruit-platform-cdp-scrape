# Security Policy

## 支持的版本

| 版本 | 支持 |
|---|---|
| 0.2.x | ✅ 持续更新 |
| 0.1.x | ⚠️ 仅安全补丁 |
| < 0.1 | ❌ 不再支持 |

## 报告漏洞

**请不要在公开 issue 里披露漏洞。** 请通过以下方式私下报告：

- GitHub Security Advisories（首选）：本仓库 → Security → Advisories → New
- 邮件：[请通过 GitHub 个人页面获取]

请在报告中包含：
- 漏洞描述与可复现步骤
- 影响范围（哪些平台 / 哪些配置）
- 你的环境（OS / Node 版本 / Python 版本）

我们承诺在 72 小时内响应。

## 安全设计

本项目基于以下安全设计原则：

1. **不模拟登录**：使用你**已登录的浏览器会话**（CDP 远程调试），不在代码里保存任何密码 / Token。
2. **不传播数据**：所有抓取结果保存在**你的本地**，不上传任何服务器。
3. **不绕风控**：遇到验证码 / 短信验证 / 滑动验证时**停下来等人手动过**，不实现自动化绕过。
5. **可移植路径**：所有路径通过环境变量注入，避免硬编码导致配置泄漏。
6. **审计可见**：所有平台动作有日志（`data/<platform>_all.jsonl` + `data/.progress_<platform>`），便于事后审计。

## 已知边界

详见 [`docs/COMPLIANCE.md`](./docs/COMPLIANCE.md)：
- 项目只用于个人使用
- 不接受任何商业化改造的 PR
- 不接受任何用于绕过反爬的 PR

## 拒绝项

项目维护者保留拒绝以下内容的权利：
- 绕过人机验证的实现
- 暴力破解 / 撞库 / 批量账号的实现
- 任何用于商业用途的功能
- 任何抓取受版权保护数据库（如 LinkedIn、Glassdoor）的 PR